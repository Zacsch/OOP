#ifndef GAME_SETUP_H
#define GAME_SETUP_H

#include <iostream>
#include <string>

using namespace std;

class game_setup
{
	public:
		game_setup();
		//no. of players (select number)
		//no. of decks (select number)
		//rotating dealer (yes/no)
		~game_setup();
};

#endif
